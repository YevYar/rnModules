/**
 * This container contains all the functions and data to dispatch them to the LoginScreen.
 *
 * @format
 */

import React from 'react';

import LoginScreen from './LoginScreen';

export default props => <LoginScreen {...props} />;
